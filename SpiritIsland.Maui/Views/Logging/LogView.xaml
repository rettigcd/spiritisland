<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 xmlns:si="clr-namespace:SpiritIsland.Maui"
			 x:DataType="si:LogModel"
			 x:Class="SpiritIsland.Maui.LogView">
	<ContentPage.Content>
		<ScrollView>
			<VerticalStackLayout>
				<!-- header -->
				<Label Text="LOG" VerticalOptions="CenterAndExpand"  HorizontalOptions="CenterAndExpand" />
				<!-- Items -->
				<CollectionView ItemsSource="{Binding Entries}">
					<CollectionView.ItemTemplate>
						<DataTemplate x:DataType="si:LogEntryModel" >
							<Label Text="{Binding text}" Margin="6,2" BackgroundColor="{Binding color}" />
						</DataTemplate>
					</CollectionView.ItemTemplate>
				</CollectionView>
			</VerticalStackLayout>
		</ScrollView>
	</ContentPage.Content>
</ContentPage>